import { boolean, integer, jsonb, pgEnum, pgTable, text, timestamp, uniqueIndex, uuid, varchar } from "drizzle-orm/pg-core";

const timestamps = { createdAt: timestamp("created_at", { withTimezone: true }).defaultNow().notNull(), updatedAt: timestamp("updated_at", { withTimezone: true }).defaultNow().notNull() };
export const workspaceStatus = pgEnum("workspace_status", ["Hoạt động", "Lưu trữ"]);
export const planStatus = pgEnum("plan_status", ["Nháp", "Chờ duyệt", "Đã duyệt", "Đang thực hiện", "Tạm dừng", "Chờ nghiệm thu", "Hoàn thành", "Đóng", "Từ chối", "Hủy"]);
export const taskStatus = pgEnum("task_status", ["Chưa thực hiện", "Đang thực hiện", "Cần hỗ trợ", "Chờ phối hợp", "Chờ phản hồi", "Chờ duyệt", "Bị chặn", "Hoàn thành", "Làm lại", "Đóng", "Hủy"]);
export const priority = pgEnum("priority", ["Cao", "Trung bình", "Thấp"]);
export const departments = pgTable("departments", { id: uuid("id").primaryKey().defaultRandom(), code: varchar("code", { length: 30 }).notNull().unique(), name: varchar("name", { length: 150 }).notNull(), ...timestamps });
export const users = pgTable("users", { id: uuid("id").primaryKey().defaultRandom(), code: varchar("code", { length: 30 }).notNull().unique(), username: varchar("username", { length: 80 }), passwordHash: varchar("password_hash", { length: 128 }), name: varchar("name", { length: 150 }).notNull(), email: varchar("email", { length: 255 }).notNull().unique(), role: varchar("role", { length: 80 }).notNull(), departmentId: uuid("department_id").references(() => departments.id), ...timestamps });
export const workspaces = pgTable("workspaces", { id: uuid("id").primaryKey().defaultRandom(), code: varchar("code", { length: 50 }).notNull().unique(), name: varchar("name", { length: 255 }).notNull(), description: text("description").notNull(), type: varchar("type", { length: 50 }).notNull(), ownerId: uuid("owner_id").references(() => users.id), status: workspaceStatus("status").notNull().default("Hoạt động"), deadline: timestamp("deadline", { withTimezone: true }), ...timestamps });
export const workspaceMembers = pgTable("workspace_members", { id: uuid("id").primaryKey().defaultRandom(), workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }), userId: uuid("user_id").notNull().references(() => users.id), role: varchar("role", { length: 50 }).notNull(), ...timestamps }, (table) => [uniqueIndex("workspace_members_workspace_user_unique").on(table.workspaceId, table.userId)]);
export const projects = pgTable("projects", { id: uuid("id").primaryKey().defaultRandom(), code: varchar("code", { length: 50 }).notNull().unique(), name: varchar("name", { length: 255 }).notNull(), workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }), ...timestamps });
export const plans = pgTable("plans", { id: uuid("id").primaryKey().defaultRandom(), code: varchar("code", { length: 50 }).notNull().unique(), name: varchar("name", { length: 255 }).notNull(), workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }), projectId: uuid("project_id").references(() => projects.id), parentId: uuid("parent_id"), module: varchar("module", { length: 100 }).notNull(), objective: text("objective").notNull(), scope: text("scope").notNull(), output: text("output").notNull(), ownerId: uuid("owner_id").references(() => users.id), approverId: uuid("approver_id").references(() => users.id), startDate: timestamp("start_date", { withTimezone: true }), deadline: timestamp("deadline", { withTimezone: true }), progress: integer("progress").notNull().default(0), status: planStatus("status").notNull().default("Nháp"), priority: priority("priority").notNull().default("Trung bình"), ...timestamps });
export const workGroups = pgTable("work_groups", { id: uuid("id").primaryKey().defaultRandom(), code: varchar("code", { length: 50 }).notNull().unique(), name: varchar("name", { length: 255 }).notNull(), planId: uuid("plan_id").notNull().references(() => plans.id, { onDelete: "cascade" }), ...timestamps });
export const tasks = pgTable("tasks", { id: uuid("id").primaryKey().defaultRandom(), code: varchar("code", { length: 50 }).notNull().unique(), name: varchar("name", { length: 255 }).notNull(), planId: uuid("plan_id").notNull().references(() => plans.id, { onDelete: "cascade" }), workspaceId: uuid("workspace_id").notNull().references(() => workspaces.id, { onDelete: "cascade" }), ownerId: uuid("owner_id").references(() => users.id), assigneeId: uuid("assignee_id").references(() => users.id), status: taskStatus("status").notNull().default("Chưa thực hiện"), priority: priority("priority").notNull().default("Trung bình"), startDate: timestamp("start_date", { withTimezone: true }), deadline: timestamp("deadline", { withTimezone: true }), progress: integer("progress").notNull().default(0), note: text("note"), actualResult: text("actual_result"), blockedReason: text("blocked_reason"), ...timestamps });
export const taskAssignees = pgTable("task_assignees", { id: uuid("id").primaryKey().defaultRandom(), taskId: uuid("task_id").notNull().references(() => tasks.id, { onDelete: "cascade" }), userId: uuid("user_id").notNull().references(() => users.id), role: varchar("role", { length: 30 }).notNull(), ...timestamps }, (table) => [uniqueIndex("task_assignees_task_user_role_unique").on(table.taskId, table.userId, table.role)]);
export const checklistItems = pgTable("checklist_items", { id: uuid("id").primaryKey().defaultRandom(), taskId: uuid("task_id").notNull().references(() => tasks.id, { onDelete: "cascade" }), content: text("content").notNull(), required: boolean("required").notNull().default(true), completed: boolean("completed").notNull().default(false), ...timestamps }, (table) => [uniqueIndex("checklist_items_task_content_unique").on(table.taskId, table.content)]);
export const subtasks = pgTable("subtasks", { id: uuid("id").primaryKey().defaultRandom(), taskId: uuid("task_id").notNull().references(() => tasks.id, { onDelete: "cascade" }), name: varchar("name", { length: 255 }).notNull(), completed: boolean("completed").notNull().default(false), ...timestamps });
export const comments = pgTable("comments", { id: uuid("id").primaryKey().defaultRandom(), taskId: uuid("task_id").notNull().references(() => tasks.id, { onDelete: "cascade" }), authorId: uuid("author_id").references(() => users.id), content: text("content").notNull(), ...timestamps });
export const attachments = pgTable("attachments", { id: uuid("id").primaryKey().defaultRandom(), entityType: varchar("entity_type", { length: 30 }).notNull(), entityId: uuid("entity_id").notNull(), fileName: varchar("file_name", { length: 255 }).notNull(), url: text("url"), ...timestamps });
export const approvals = pgTable("approvals", { id: uuid("id").primaryKey().defaultRandom(), entityType: varchar("entity_type", { length: 30 }).notNull(), entityId: uuid("entity_id").notNull(), action: varchar("action", { length: 50 }).notNull(), approverId: uuid("approver_id").references(() => users.id), note: text("note"), status: varchar("status", { length: 30 }).notNull(), ...timestamps });
export const activityLogs = pgTable("activity_logs", { id: uuid("id").primaryKey().defaultRandom(), entityType: varchar("entity_type", { length: 30 }).notNull(), entityId: uuid("entity_id").notNull(), actorId: uuid("actor_id").references(() => users.id), action: varchar("action", { length: 100 }).notNull(), description: text("description"), metadata: jsonb("metadata"), ...timestamps });
export const riskIssues = pgTable("risk_issues", { id: uuid("id").primaryKey().defaultRandom(), planId: uuid("plan_id").notNull().references(() => plans.id, { onDelete: "cascade" }), title: varchar("title", { length: 255 }).notNull(), severity: priority("severity").notNull().default("Trung bình"), status: varchar("status", { length: 30 }).notNull().default("Mở"), ...timestamps });
export const notifications = pgTable("notifications", { id: uuid("id").primaryKey().defaultRandom(), userId: uuid("user_id").notNull().references(() => users.id), title: varchar("title", { length: 255 }).notNull(), content: text("content").notNull(), read: boolean("read").notNull().default(false), ...timestamps });
